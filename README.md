## Source Tutorial
[Flask Full Course: Build Stunning Web Apps Fast | Python Flask Tutorial](https://youtu.be/45P3xQPaYxc?si=4xGLbpB2oEhGDVUF)

## Tech Stack
- Python
- Flask
- SQL (using SQLAlchemy)
- HTML/CSS (using Sass)

## Installation
Before running the app, install the needed requirements by running this command:
```
pip install -r requirements.txt
```

## Personal Learning Points
- **CRUD Routing on Flask using `@app.route()`**  
   This skill is somewhat transferrable from Spring Boot mapping (`@RequestMapping`, `@GetMapping`, `@PostMapping`, etc) but last time I was on Spring Boot I forgot all my annotations. Needless to say the backend broke 😔

- **HTML Template Inheritance**  
  I didn't know they could do that but I like that they could. Still trying to get used to the `{% %}` formatting but kinda hate `{{ }}` for dynamic content. I count my braces like eggs in a basket 🥚

- **Sassy CSS**  
  I love it, especially the nesting. It's cute and intuitive. Got lost looking for my Sass Watcher only to realise I didn't download the live compiler extension. Either I missed a part of the tutorial or it was omitted, but nothing Google can't fix 💓

## What's Next?
- **Microservices**  
  After all, this is *just* a simple task manager webapp. If it weren't for my past self forgetting to download all of my prof's lab files I would've tried making a more complex app. Maybe when I find another tutorial... 🤔
  
- **Learning Django**  
  Django is huge and industry standard (and why I feel underqualified for jobs). I'm under the impression it's like Spring Boot for Python or Flask on steroids, so maybe one day at a time 📆
